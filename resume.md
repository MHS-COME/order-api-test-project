# 王同学

📞 138-xxxx-xxxx | ✉️ mhscome@xxx.com | 📍 青岛 | 🎂 2002.08 | 🎓 某某大学 计算机科学与技术 2026届本科

---

## 项目经验

### OrderAPI 全栈自动化测试平台（个人项目）
*2025.03 — 至今*

**一句话：用 pytest + CI/CD 搭了一套能自动跑测试、自动出报告、自动提 Bug 的完整测试体系，覆盖接口/性能/UI 三层。**

- **写了 38 条 API 自动化用例**，覆盖登录认证、订单 CRUD、支付全链路，**正反向+边界+越权+状态冲突全部覆盖**，不是只测 happy path
- **用 pytest 夹具（fixture）抽掉了 5 处重复的订单创建代码**，没有这层抽象，每加一个用例就要多写 6 行样板代码
- **接入了真实的微信公众平台 API**（非 Mock），写了 token 管理/菜单/素材/模板消息/用户信息 5 个模块 26 条用例，**对接第三方真实接口的经验不是每个实习生都有的**
- **用 k6 写了性能脚本**，从 50 并发拉到 500 并发，能说出来 QPS 瓶颈在哪
- **Selenium 自动化了登录/下单/支付 3 条核心 UI 流程**，不是只会跑接口
- **Docker 容器化 + GitHub Actions CI/CD**：项目已打包成 Docker 镜像（node:18-alpine），workflow 里测试通过后自动构建镜像，**从代码提交到容器交付的链路全跑通**
- **Postman Collection + Newman 命令行也能跑**，面试官你换工具我也能用

**面试官你直接问**：fixture scope 怎么选的？为什么 JWT 要双 token？订单超时支付的错误码是什么？我都能答。


### WeChatAPITest 微信公众平台接口测试（个人项目）
*2025.05 — 至今*

**一句话：用真实的 AppID/AppSecret 调用微信开放 API，验证 token 生命周期、菜单 CRUD、素材上传、模板消息推送，所有断言对接微信官方错误码规范。**

- 不是调自己写的 Mock，**是微信真服务器**，网络超时、限流、token 过期这些 Mock 不会出现的问题全踩过
- token 模块验了获取/过期/错误凭证 5 种场景，**微信的错误码 40001/40125 都背下来了**
- 菜单创建遇到 40033 中文编码坑，**通过构造英文字段绕过，写进了测试记录**——出问题能复盘
- 素材模块测了图片/语音/视频三种类型上传+分页查询，**文件上传类接口的测试思路和普通 JSON 接口不一样，我做过**

---

## 技能清单

| 类别 | 具体内容 |
|------|---------|
| **测试框架** | pytest（夹具/参数化/conftest）、Selenium、k6、Postman/Newman |
| **CI/CD** | GitHub Actions 流水线编排、Docker 容器化、Allure 报告生成、TAPD API 缺陷归档 |
| **协议/工具** | HTTP/JWT 认证、RESTful API、Git、Charles/Burp Suite 抓包 |
| **语言** | Python（主力）、SQL（增删改查+联表）、HTML/CSS/JS（能写测试看板） |
| **软技能** | 独立踩坑→定位→修→复盘闭环、用 AI 辅助写代码提效、技术文档写作 |

---

## 自我评价

普通二本，没有实习。所以我用 **两个完整项目** 证明我能干活：

- 不是只会写 `assert resp.status_code == 200`——我知道 401/403/409 各代表什么，我知道参数化消除重复，我知道 token 过期要 refresh 而不是重新登录
- 不是只测过 CRUD——支付状态机（PENDING→PAID→SHIPPED/CANCELLED）的状态转换冲突我全写了用例
- 不是 Mock 选手——微信真实 API 的坑我踩过，线上接口才有的超时/限流/编码问题我有概念
- **我的底线：进来了不会从零教，第一周就能跟着写用例**

---

> 💡 **面试建议**：面试官如果担心应届生白纸一张，可以直接对着项目问——我的 GitHub 代码是公开的，每个用例为什么这么写我都能讲出理由。
