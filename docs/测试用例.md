# OrderAPITest — 测试用例清单

> 版本：v2.0  
> 更新时间：2026-06-15  
> 设计方法：等价类划分、边界值分析、场景法、状态机测试

---

## 1. 用例概览

| 模块 | 正向用例 | 反向用例 | 合计 |
|------|---------|---------|------|
| 用户注册 `POST /register` | 2 | 4 | 6 |
| 用户登录 `POST /login` | 5 | 5 | 10 |
| Token 刷新 `POST /refresh` | 1 | 3 | 4 |
| 创建订单 `POST /orders` | 5 | 8 | 13 |
| 订单列表 `GET /orders` | 1 | — | 1 |
| 查询订单 `GET /orders/{id}` | 3 | 5 | 8 |
| 取消订单 `PUT /orders/{id}/cancel` | 4 | 6 | 10 |
| 发起支付 `POST /payment` | 1 | 8 | 9 |
| 查询支付 `GET /payment/{orderId}` | 1 | — | 1 |
| 模拟支付 `POST /payment/simulate` | 1 | 1 | 2 |
| 全链路 E2E | 1 | — | 1 |
| **API 小计** | **25** | **40** | **65** |
| 安全测试 | — | 14 | 14 |
| Selenium UI 测试 | 13 | 8 | 21 |
| **总计** | **38** | **62** | **100** |

> API 测试通过 6 个 Postman Collection 模块执行（00-注册、01-登录、02-订单、03-E2E、04-支付、05-安全），合计 36 个请求、191 条断言。

---

## 2. 测试数据约定

| 变量 | 值 | 说明 |
|------|-----|------|
| `${BASE_URL}` | `http://localhost:3000` | Mock 服务地址 |
| `${VALID_USER}` | `testuser` | 合法用户名 |
| `${VALID_PWD}` | `Test@123456` | 合法密码 |
| `${OTHER_USER}` | `otheruser` | 另一合法用户（用于越权测试） |
| `${OTHER_PWD}` | `Other@123456` | 另一用户密码 |
| `${VALID_TOKEN}` | 登录后获取 | 有效 Bearer Token（access，1h） |
| `${REFRESH_TOKEN}` | 登录后获取 | 有效 Refresh Token（7d） |
| `${OTHER_TOKEN}` | 另一用户登录获取 | 用于越权测试 |
| `${VALID_PRODUCT_ID}` | `5001` | 有效商品 ID（无线蓝牙耳机） |
| `${VALID_ADDRESS_ID}` | `100` | 有效收货地址 ID |

**CSV 数据驱动：**
- `postman/data/login_data.csv` — 7 组用户名/密码组合（覆盖合法、边界、错误凭证）
- `postman/data/order_data.csv` — 6 组商品/数量/地址组合（覆盖正向和边界值）

---

## 3. 等价类划分

### 3.1 用户名（username）

| 等价类 | 范围 | 示例 |
|--------|------|------|
| 有效 | 4-32 字符 | `testuser` |
| 无效 — 太短 | < 4 字符 | `abc` |
| 无效 — 太长 | > 32 字符 | 33 位字符串 |
| 无效 — 空 | 空字符串或不传 | `""` |

### 3.2 密码（password）

| 等价类 | 范围 | 示例 |
|--------|------|------|
| 有效（注册） | 6-20 字符 | `Test@123456` |
| 有效（登录） | 6-64 字符 | `Test@123456` |
| 无效 — 太短 | < 6 字符 | `12345` |
| 无效 — 太长（注册） | > 20 字符 | 21 位字符串 |
| 无效 — 太长（登录） | > 64 字符 | 65 位字符串 |
| 无效 — 空 | 空字符串或不传 | `""` |

### 3.3 购买数量（quantity）

| 等价类 | 范围 | 示例 |
|--------|------|------|
| 有效下边界 | `1` | `1` |
| 有效上边界 | `999` | `999` |
| 有效中间值 | 2-998 | `5` |
| 无效下边界 | `< 1` | `0` |
| 无效上边界 | `> 999` | `1000` |

### 3.4 支付金额（amount）

| 等价类 | 范围 | 示例 |
|--------|------|------|
| 有效 | 等于订单 totalAmount | `299.00` |
| 无效 — 不匹配 | 不等于订单金额 | `199.00` |
| 无效 — 空 | 不传 | — |

---

## 4. 详细测试用例

### 4.1 用户注册 — `POST /register`

| 用例ID | 测试点 | 输入数据 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-REG-001 | 合法注册成功 | `{"username":"newuser","password":"Test@123","email":"new@test.com"}` | ① HTTP 200<br>② `code`=0<br>③ `data.id` 为正整数<br>④ `data.username` = 请求的 username | 正向 |
| TC-REG-002 | 仅必填字段（不含 email） | `{"username":"user2","password":"Pass@123"}` | ① HTTP 200<br>② `data.email` = null | 正向 |
| TC-REG-003 | 缺少 username | `{"password":"Test@123"}` | ① HTTP 400<br>② `code`=1001<br>③ `message` 包含 "username" | 反向-必填 |
| TC-REG-004 | 缺少 password | `{"username":"user3"}` | ① HTTP 400<br>② `code`=1001<br>③ `message` 包含 "password" | 反向-必填 |
| TC-REG-005 | 用户名已存在 | `{"username":"testuser","password":"Test@123"}` | ① HTTP 409<br>② `code`=1003<br>③ `message`="用户名已存在" | 反向-重复 |
| TC-REG-006 | username 过短（3 字符） | `{"username":"abc","password":"Test@123"}` | ① HTTP 400<br>② `code`=1001<br>③ `message` 含长度限制 | 反向-边界值 |

### 4.2 用户登录 — `POST /login`

#### 正向用例

| 用例ID | 测试点 | 输入数据 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-LOGIN-001 | 合法凭证登录 | `{"username":"testuser","password":"Test@123456"}` | ① HTTP 200<br>② `code`=0<br>③ `data.token` 非空（合法 JWT）<br>④ `data.refreshToken` 非空<br>⑤ `data.expiresIn`=3600<br>⑥ `data.tokenType`="Bearer" | 正向 |
| TC-LOGIN-002 | username 边界值（4 字符） | `{"username":"abcd","password":"T@1234"}` | ① HTTP 200<br>② `code`=0 | 正向-边界值 |
| TC-LOGIN-003 | username 边界值（32 字符） | 32 字符用户名 + 合法密码 | ① HTTP 200<br>② `code`=0 | 正向-边界值 |
| TC-LOGIN-004 | password 边界值（6 字符） | `{"username":"abcd","password":"T@1234"}` | ① HTTP 200<br>② `code`=0 | 正向-边界值 |
| TC-LOGIN-005 | password 边界值（64 字符） | `{"username":"pwdtest","password":"<64 字符>"}` | ① HTTP 200<br>② `code`=0 | 正向-边界值 |

#### 反向用例

| 用例ID | 测试点 | 输入数据 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-LOGIN-006 | 缺少 username | `{"password":"Test@123456"}` | ① HTTP 400<br>② `code`=1001 | 反向-必填 |
| TC-LOGIN-007 | 缺少 password | `{"username":"testuser"}` | ① HTTP 400<br>② `code`=1001 | 反向-必填 |
| TC-LOGIN-008 | 密码错误 | `{"username":"testuser","password":"WrongPwd"}` | ① HTTP 401<br>② `code`=1002 | 反向-凭证 |
| TC-LOGIN-009 | username 过短（3 字符） | `{"username":"abc","password":"T@1234"}` | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |
| TC-LOGIN-010 | password 过短（5 字符） | `{"username":"testuser","password":"T@123"}` | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |

### 4.3 Token 刷新 — `POST /refresh`

| 用例ID | 测试点 | 输入数据 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-REF-001 | 刷新成功 | `{"refreshToken":"<有效 refreshToken>"}` | ① HTTP 200<br>② `code`=0<br>③ 返回新 access token | 正向 |
| TC-REF-002 | 缺少 refreshToken | `{}` | ① HTTP 400<br>② `code`=1001 | 反向-必填 |
| TC-REF-003 | refreshToken 无效 | `{"refreshToken":"invalid"}` | ① HTTP 401<br>② `code`=2001 | 反向-认证 |
| TC-REF-004 | refreshToken 已撤销 | 重新登录后的旧 refreshToken | ① HTTP 401<br>② `code`=2001 | 反向-认证 |

### 4.4 创建订单 — `POST /orders`

#### 正向用例

| 用例ID | 测试点 | 输入数据 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-ORDER-001 | 全字段创建 | `{"productId":5001,"quantity":2,"addressId":100,"remark":"请尽快发货"}` | ① HTTP 201<br>② `code`=0<br>③ `data.status`="PENDING"<br>④ `data.totalAmount`=299.00<br>⑤ `data.orderNo` 以 "ORD" 开头 | 正向 |
| TC-ORDER-002 | 仅必填字段 | `{"productId":5001,"quantity":1,"addressId":100}` | ① HTTP 201<br>② `data.remark`=null | 正向 |
| TC-ORDER-003 | quantity 下边界（1） | `{"productId":5001,"quantity":1,"addressId":100}` | ① HTTP 201<br>② `data.quantity`=1 | 正向-边界值 |
| TC-ORDER-004 | quantity 上边界（999） | `{"productId":5002,"quantity":999,"addressId":100}` | ① HTTP 201<br>② `data.quantity`=999 | 正向-边界值 |
| TC-ORDER-005 | remark 上边界（200 字符） | remark 恰好 200 字符 | ① HTTP 201<br>② remark 完整保存 | 正向-边界值 |

#### 反向用例

| 用例ID | 测试点 | 输入数据 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-ORDER-006 | 无 Authorization 头 | 无 Token | ① HTTP 401<br>② `code`=2001 | 反向-认证 |
| TC-ORDER-007 | Token 已过期 | 过期 Token | ① HTTP 401<br>② `code`=2002 | 反向-认证 |
| TC-ORDER-008 | Token 被篡改 | 篡改的 JWT | ① HTTP 401<br>② `code`=2001 | 反向-认证 |
| TC-ORDER-009 | 缺少 productId | `{"quantity":1,"addressId":100}` | ① HTTP 400<br>② `code`=1001 | 反向-必填 |
| TC-ORDER-010 | productId 为 0 | `{"productId":0,"quantity":1,"addressId":100}` | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |
| TC-ORDER-011 | productId 为负数 | `{"productId":-1,"quantity":1,"addressId":100}` | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |
| TC-ORDER-012 | quantity 为 0 | `{"productId":5001,"quantity":0,"addressId":100}` | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |
| TC-ORDER-013 | quantity 为 1000 | `{"productId":5001,"quantity":1000,"addressId":100}` | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |

### 4.5 订单列表 — `GET /orders`

| 用例ID | 测试点 | 前置条件 | 预期结果 | 类型 |
|--------|--------|---------|---------|------|
| TC-LIST-001 | 查询当前用户订单列表 | 已登录，用户有订单 | ① HTTP 200<br>② `data.orders` 为数组<br>③ `data.total` 正确<br>④ `data.userId`=当前用户 ID<br>⑤ 按创建时间倒序 | 正向 |

### 4.6 查询订单详情 — `GET /orders/{id}`

| 用例ID | 测试点 | 预期结果 | 类型 |
|--------|--------|---------|------|
| TC-GET-001 | 查询本人存在的订单 | ① HTTP 200<br>② `data.status`="PENDING"<br>③ `data.items` 数组非空<br>④ `data.address` 对象非空 | 正向 |
| TC-GET-002 | 查询多商品订单 | ① `data.items` 长度 = 商品数<br>② subtotal = price × quantity | 正向 |
| TC-GET-003 | 查询已取消订单 | ① `data.status`="CANCELLED"<br>② 订单详情正常返回 | 正向 |
| TC-GET-004 | 订单 ID 不存在 | ① HTTP 404<br>② `code`=3001 | 反向-资源 |
| TC-GET-005 | 查询非本人订单（越权） | ① HTTP 403<br>② `code`=3002 | 反向-越权 |
| TC-GET-006 | 订单 ID 为非数字 | ① HTTP 400<br>② `code`=1001 | 反向-格式 |
| TC-GET-007 | 订单 ID 为 0 | ① HTTP 400<br>② `code`=1001 | 反向-边界值 |
| TC-GET-008 | 无 Authorization 头 | ① HTTP 401<br>② `code`=2001 | 反向-认证 |

### 4.7 取消订单 — `PUT /orders/{id}/cancel`

| 用例ID | 测试点 | 预期结果 | 类型 |
|--------|--------|---------|------|
| TC-CANCEL-001 | 取消 PENDING 状态订单 | ① HTTP 200<br>② `data.status`="CANCELLED"<br>③ `data.cancelledAt` 有效 | 正向 |
| TC-CANCEL-002 | 附取消原因取消 | ① HTTP 200<br>② 取消成功 | 正向 |
| TC-CANCEL-003 | reason 边界值（200 字符） | ① HTTP 200<br>② reason 完整保存 | 正向-边界值 |
| TC-CANCEL-004 | 验证取消后持久化 | ① 再次 GET 确认 status="CANCELLED" | 正向-状态验证 |
| TC-CANCEL-005 | 取消不存在的订单 | ① HTTP 404<br>② `code`=3001 | 反向-资源 |
| TC-CANCEL-006 | 取消非本人订单（越权） | ① HTTP 403<br>② `code`=3002 | 反向-越权 |
| TC-CANCEL-007 | 取消已发货订单（SHIPPED） | ① HTTP 409<br>② `code`=4001<br>③ `data.currentStatus`="SHIPPED" | 反向-状态冲突 |
| TC-CANCEL-008 | 重复取消（幂等性） | ① HTTP 409<br>② `code`=4002 | 反向-幂等性 |
| TC-CANCEL-009 | 取消已支付订单（PAID） | ① HTTP 409<br>② `code`=4005<br>③ `message` 含 "已支付" | 反向-状态冲突 |
| TC-CANCEL-010 | 无 Authorization 头 | ① HTTP 401<br>② `code`=2001 | 反向-认证 |

### 4.8 发起支付 — `POST /payment`

| 用例ID | 测试点 | 预期结果 | 类型 |
|--------|--------|---------|------|
| TC-PAY-001 | 正常支付 PENDING 订单 | ① HTTP 200<br>② `data.status`="PAID"<br>③ `data.transactionId` 以 "TXN" 开头<br>④ `data.paidAt` 有效 | 正向 |
| TC-PAY-002 | 缺少 orderId | ① HTTP 400<br>② `code`=1001 | 反向-必填 |
| TC-PAY-003 | 缺少 amount | ① HTTP 400<br>② `code`=1001 | 反向-必填 |
| TC-PAY-004 | 订单不存在 | ① HTTP 404<br>② `code`=3001 | 反向-资源 |
| TC-PAY-005 | 支付非本人订单（越权） | ① HTTP 403<br>② `code`=3002 | 反向-越权 |
| TC-PAY-006 | 重复支付（幂等性） | ① HTTP 409<br>② `code`=4003<br>③ `message`="订单已支付" | 反向-幂等性 |
| TC-PAY-007 | 支付已取消订单 | ① HTTP 409<br>② `code`=4003 | 反向-状态冲突 |
| TC-PAY-008 | 订单超时关闭（> 30min） | ① HTTP 409<br>② `code`=4004<br>③ `message` 含 "超时关闭" | 反向-超时 |
| TC-PAY-009 | 金额与订单不符 | ① HTTP 400<br>② `code`=1005<br>③ `message` 含 "金额" | 反向-金额校验 |

### 4.9 查询支付状态 — `GET /payment/{orderId}`

| 用例ID | 测试点 | 预期结果 | 类型 |
|--------|--------|---------|------|
| TC-PAYQ-001 | 查询已支付订单状态 | ① HTTP 200<br>② `data.status`="PAID"<br>③ `data.transactionId` 非空 | 正向 |

### 4.10 模拟支付 — `POST /payment/simulate`

| 用例ID | 测试点 | 预期结果 | 类型 |
|--------|--------|---------|------|
| TC-SIM-001 | 模拟支付成功（result=SUCCESS） | ① HTTP 200<br>② `data.status`="PAID" | 正向 |
| TC-SIM-002 | 模拟支付失败（result=FAIL） | ① HTTP 400<br>② `code`=4006 | 反向 |

### 4.11 全链路 E2E

| 用例ID | 测试步骤 | 预期结果 | 类型 |
|--------|---------|---------|------|
| TC-E2E-001 | 1. 登录获取 Token<br>2. 创建订单获取 orderId<br>3. 发起支付<br>4. 查询支付状态确认 PAID<br>5. 查询订单详情验证数据一致性 | ① 步骤1: 返回有效双 token<br>② 步骤2: status=PENDING<br>③ 步骤3: status=PAID, transactionId 非空<br>④ 步骤4: 支付状态正确<br>⑤ 步骤5: 数据一致<br>⑥ 全链路 ≤ 3s | E2E |

---

## 5. 安全测试用例

| 用例ID | 测试点 | 攻击向量 | 预期结果 |
|--------|--------|---------|---------|
| TC-SEC-001 | 无 Token 访问受保护接口 | 不携带 Authorization 头请求 `/orders` | HTTP 401, code=2001 |
| TC-SEC-002 | 过期 Token 访问 | 使用 `GET /test/expired-token` 获取的过期 token | HTTP 401, code=2002 |
| TC-SEC-003 | 篡改 Token | 修改 JWT payload 中 userId 后重签名 | HTTP 401, code=2001 |
| TC-SEC-004 | none 算法攻击 | JWT header `{"alg":"none"}` | HTTP 401, 服务端强制 HS256 |
| TC-SEC-005 | 签名缺失 | JWT 移除签名部分（仅 header.payload） | HTTP 401, code=2001 |
| TC-SEC-006 | SQL 注入 — 登录 | username=`' OR '1'='1` | HTTP 400/401（参数校验或凭证错误） |
| TC-SEC-007 | SQL 注入 — 订单查询 | id=`1' OR '1'='1` | HTTP 400（非数字参数校验拦截） |
| TC-SEC-008 | XSS 脚本注入 | remark=`<script>alert(1)</script>` | HTTP 200/201，输出 HTML 实体转义 |
| TC-SEC-009 | 参数污染 | 同时发送多个同名参数 | 取第一个值，正常处理 |
| TC-SEC-010 | 水平越权 — 查询订单 | 用户 A 的 Token 查询用户 B 的订单（20001） | HTTP 403, code=3002 |
| TC-SEC-011 | 水平越权 — 取消订单 | 用户 A 的 Token 取消用户 B 的订单 | HTTP 403, code=3002 |
| TC-SEC-012 | 水平越权 — 支付订单 | 用户 A 的 Token 支付用户 B 的订单 | HTTP 403, code=3002 |
| TC-SEC-013 | Bearer 前缀移除 | 直接传 token 字符串（无 "Bearer " 前缀） | HTTP 401, code=2001 |
| TC-SEC-014 | 空 Token | Authorization 头值为空字符串 | HTTP 401, code=2001 |

---

## 6. Selenium UI 测试用例

### 6.1 TestLoginUI — 登录页面（3 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-LOGIN-001 | test_login_success | 打开应用 → 导航到登录页 → 填写正确凭证 → 点击登录 | Token 文本更新为非空 JWT，页面跳转到仪表盘 |
| TC-UI-LOGIN-002 | test_login_wrong_password | 打开应用 → 导航到登录页 → 填写错误密码 → 点击登录 | 显示错误提示信息 |
| TC-UI-LOGIN-003 | test_login_empty_fields | 打开应用 → 导航到登录页 → 不填写字段 → 点击登录 | 显示字段验证提示 |

### 6.2 TestRegisterUI — 注册页面（2 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-REG-001 | test_register_success | 导航到注册页 → 填写新用户名/密码 → 点击注册 | 显示注册成功提示 |
| TC-UI-REG-002 | test_register_username_too_short | 导航到注册页 → 填写 3 字符用户名 → 点击注册 | 显示用户名长度校验提示 |

### 6.3 TestCreateOrderUI — 创建订单（2 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-ORDER-001 | test_create_order_success | 登录 → 导航到创建订单页 → 填写商品/数量/地址 → 创建 | 订单创建成功提示 |
| TC-UI-ORDER-002 | test_create_order_no_login | 未登录 → 导航到创建订单页 → 填写并创建 | 拦截提示（需登录） |

### 6.4 TestOrderListUI — 订单列表（5 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-LIST-001 | test_list_loads_after_login | 登录 → 导航到订单列表 | 表格渲染，订单行数 ≥ 7 |
| TC-UI-LIST-002 | test_paid_order_no_cancel_button | 登录 → 加载订单列表 → 定位 PAID 订单（20012） | 操作列显示"不可取消"，无取消按钮 |
| TC-UI-LIST-003 | test_cancelled_order_no_operation | 登录 → 加载订单列表 → 定位 CANCELLED 订单 | 操作列显示"不可操作" |
| TC-UI-LIST-004 | test_status_badges_display | 登录 → 加载订单列表 → 检查状态标签 | PENDING/PAID/CANCELLED/SHIPPED 标签正确显示 |
| TC-UI-LIST-005 | test_pay_button_navigates_to_payment | 登录 → 加载列表 → 点击某 PENDING 订单的支付按钮 | 导航到支付页面并自动填充订单号 |

### 6.5 TestCancelBoundary — 取消边界（2 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-CANCEL-001 | test_cancel_paid_order_via_page_fails | 登录 → 导航到取消页 → 输入 PAID 订单号 → 点击取消 | 返回错误（4005/已支付/不允许） |
| TC-UI-CANCEL-002 | test_cancel_cancelled_order_via_page_fails | 登录 → 导航到取消页 → 输入 CANCELLED 订单号 → 点击取消 | 返回错误（4002/已被取消） |

### 6.6 TestPaymentUI — 支付页面（2 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-PAY-001 | test_payment_success | 登录 → 导航到支付页 → 填写 PENDING 订单号和金额 → 支付 | 支付成功提示 |
| TC-UI-PAY-002 | test_payment_empty_orderid | 登录 → 导航到支付页 → 不填订单号 → 点击支付 | 显示订单号必填校验 |

### 6.7 TestFullE2E — 端到端（1 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-E2E-001 | test_full_flow | 打开应用 → 登录 → 创建订单 → 加载订单列表 → 找到新订单 → 支付 → 列表确认状态 | 全链路每一步均成功，订单最终状态为 PAID |

### 6.8 TestNavigation — 侧边栏导航（1 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-NAV-001 | test_nav_switch_pages | 启动应用 → 依次点击侧边栏 7 个页面链接 | 每个页面对应 page 区域正确激活显示 |

### 6.9 TestDashboard — 仪表盘（2 用例）

| 用例ID | 测试方法 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-UI-DASH-001 | test_dashboard_stats | 登录 → 查看仪表盘统计卡片 | 总订单数、待支付数、已支付数正确显示 |
| TC-UI-DASH-002 | test_health_indicator | 查看健康检查指示灯 | 显示"正常"状态（Mock 服务在线） |

---

## 7. 用例设计方法对应

| 用例范围 | 设计方法 | 说明 |
|---------|---------|------|
| TC-REG-001~006 | 等价类 + 边界值 | 覆盖注册字段的有效/无效等价类和边界 |
| TC-LOGIN-001~010 | 等价类 + 边界值 + DDT | CSV 数据驱动 7 组正交组合 |
| TC-ORDER-001~013 | 等价类 + 边界值 | quantity 有效范围 1-999 全边界覆盖 |
| TC-CANCEL-007~009 | 状态机 | 不合法状态转换（SHIPPED→取消、PAID→取消、CANCELLED→取消） |
| TC-PAY-006~008 | 状态机 + 幂等性 | 重复支付拒止、超时关闭、已取消不可支付 |
| TC-SEC-001~014 | 安全测试（OWASP 参考） | SQL 注入、XSS、越权、JWT 攻击、参数污染 |
| TC-UI-* | 场景法 + 状态机 | 模拟真实用户操作路径 + 订单状态 UI 行为验证 |
| TC-E2E-001 | 场景法 | API + UI 完整业务链路串联 |

---

## 8. 用例执行顺序

### API 测试（CI 6 模块并行）

```
模块 00-注册 → 模块 01-登录 → 模块 02-订单 → 模块 04-支付 → 模块 03-E2E
                                    ↘ 模块 05-安全测试（可并行）
```

### UI 测试执行顺序

```
TestNavigation → TestLoginUI → TestRegisterUI → TestCreateOrderUI
→ TestOrderListUI → TestCancelBoundary → TestPaymentUI
→ TestDashboard → TestFullE2E
```

> 说明：UI 测试按依赖关系排序 — 先验证导航和登录，再测试功能页面，E2E 全链路最后执行。
